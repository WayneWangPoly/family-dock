{
  "name": "family-dock-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0",
    "openai": "^6.10.0",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "@types/web-push": "^3.6.4"
  },
  "engines": {
    "node": "22"
  }
}
