# Consumer Polish v6

Small optional polish after the main mobile cleanup:

- Payments: hide paid items under Paid history; rename `Undo paid` to `Mark unpaid`.
- Requests: remove large dashboard stats; pending requests are first; past requests are folded.
- Requests: conditional approval is hidden behind `Approve with note`.
- Child/homestay portal: remove realtime/debug-style header controls; shorten homework item-type labels.

No Supabase changes are required.
