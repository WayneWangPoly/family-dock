/*
Optional integration example.

If your current CalendarPanel already has a settings area, import:

import { CalendarIntegrationPanel } from "./CalendarIntegrationPanel";

Then render:

<CalendarIntegrationPanel data={data} />

Do NOT blindly replace your existing CalendarPanel if it already contains mature month/week/day views.
This patch package intentionally provides an additive panel so you can mount it where appropriate.
*/
export {};
