/*
Optional integration example.

In your existing Health / QA page, import:

import { ProductionHardeningPanel } from "./ProductionHardeningPanel";

Then render:

<ProductionHardeningPanel data={data} />

This pack is additive and should not replace your existing Health page.
*/
export {};
